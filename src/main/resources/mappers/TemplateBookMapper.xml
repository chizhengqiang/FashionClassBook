<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.conan.fashionclassbook.dao.TemplateBookMapper">

    <resultMap id="BaseResultMap" type="com.conan.fashionclassbook.pojo.TemplateBook">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="image" property="image" jdbcType="VARCHAR"/>
        <result column="price" property="price" jdbcType="NUMERIC"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="createTime" property="createTime" jdbcType="TIMESTAMP"></result>
        <result column="lastTime" property="lastTime" jdbcType="TIMESTAMP"></result>
    </resultMap>

    <sql id="Base_Column_List">
        id, `name`, `title`, `image`, price, `status`, createTime, lastTime
    </sql>

    <sql id="tableName">
        template_book
    </sql>

    <select id="findAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from
        <include refid="tableName"/>
        where status != 9
    </select>

    <select id="getById" resultMap="BaseResultMap" parameterType="int">
        select
        <include refid="Base_Column_List"/>
        from
        <include refid="tableName"/>
        where status != 9 AND id = #{id}
    </select>

    <insert id="insertSelective" parameterType="com.conan.fashionclassbook.pojo.Banner">
        insert into
        <include refid="tableName"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="name != null">
                `name`,
            </if>
            <if test="title != null">
                `title`,
            </if>
            <if test="image != null">
                `image`,
            </if>
            <if test="price != null">
                `price`,
            </if>
            <if test="status != null">
                `status`,
            </if>
            <if test="createTime != null">
                createTime,
            </if>
            <if test="lastTime != null">
                lastTime,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="title != null">
                #{title,jdbcType=VARCHAR},
            </if>
            <if test="image != null">
                #{image,jdbcType=VARCHAR},
            </if>
            <if test="price != null">
                #{price,jdbcType=NUMERIC},
            </if>
            <if test="status != null">
                #{status,jdbcType=SMALLINT},
            </if>
            <if test="createTime != null">
                #{createtime,jdbcType=TIMESTAMP},
            </if>
            <if test="lastTime != null">
                #{lasttime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>

    <select id="selectByPrimaryKey" parameterType="int" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from
        <include refid="tableName"/>
        where id = #{id}
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.conan.fashionclassbook.pojo.Banner">
        update
        <include refid="tableName"/>
        <set>
            <if test="name != null">
                `name` = #{name,jdbcType=VARCHAR},
            </if>
            <if test="title != null">
                `title` = #{title,jdbcType=VARCHAR},
            </if>
            <if test="image != null">
                `image` = #{image,jdbcType=VARCHAR},
            </if>
            <if test="type != null">
                `type` = #{type,jdbcType=SMALLINT},
            </if>
            <if test="status != null">
                `status` = #{status,jdbcType=SMALLINT},
            </if>
            <if test="createTime != null">
                createTime = #{createTime,jdbcType=TIMESTAMP},
            </if>
            <if test="lastTime != null">
                lastTime = #{lastTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <update id="changeStatusById" parameterType="map">
        update
        <include refid="tableName"/>
        set `status` = #{status} where id = #{id}
    </update>

</mapper>